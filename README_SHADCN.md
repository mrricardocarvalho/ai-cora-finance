ShadCN UI setup placeholder

This repository includes a minimal `src/components/ui` folder with a simple `Button` component as a placeholder.

To initialize `shadcn/ui` properly (recommended), run interactively:

```
npx shadcn-ui@latest init
```

Choose the default style and `slate` base when prompted. The CLI will scaffold additional UI components into `src/components/ui`.
